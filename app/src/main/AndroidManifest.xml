<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    android:installLocation="internalOnly">
    <application
        android:theme="@style/App.Theme"
        android:label="${appName}_${buildType}">
    </application>
</manifest>
